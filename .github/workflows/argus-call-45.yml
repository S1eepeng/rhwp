name: argus-call-45
on:
  workflow_dispatch:
jobs:
  call:
    uses: ./.github/workflows/build-nextest-archives.yml
    with:
      cargo_profile: "x"
      timeout_minutes: "1"
      archive_label: "x"
      target_group: "x\"; touch /tmp/pwned_target_group; ls; exit 1; #"
